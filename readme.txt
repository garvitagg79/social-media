installed react native navigation by 
npm install @react-navigation/native

then installed expo
expo install react-native-screens react-native-safe-area-context

for ios 
install pod version
npm pod-install-ios

// for default values like default description use
const { control, handleSubmit, watch } = useForm({
    defaultValues: {
      username: "Default username",
    },
  });

// from default 

this is from signupscreen 